# Core scientific stack
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0

# Time-series, econometrics, and data APIs
statsmodels>=0.14,<1.0
arch>=6.0,<7.0
yfinance>=0.2,<1.0
pandas-datareader>=0.10,<1.0

# Visualization & reporting
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0

# Data access / serialization / utilities
requests>=2.31,<3.0
pyarrow>=12.0,<15.0
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
click>=8.1,<9.0
tqdm>=4.66,<5.0

# LLM + Ollama client
ollama>=0.1,<1.0

# Optional GPU acceleration for MSSA-RL (Linux CUDA hosts only)
cupy-cuda12x>=13.0 ; platform_system == "Linux"

# Database optional (PostgreSQL support)
psycopg[binary]>=3.1,<4.0

# Testing
pytest>=7.4,<9.0
