# Signal Routing Configuration
# Time Series as Default Signal Generator, LLM as Fallback

signal_routing:
  # Primary signal source
  time_series_primary: true  # DEFAULT: true - Time Series is primary

  # Fallback/redundancy
  llm_fallback: false  # Disable LLM fallback to speed up runs
  llm_redundancy: false  # Disable redundancy to avoid latency overhead

  # Deprecated routing knobs (currently no-op in SignalRouter).
  # They remain for backward compatibility and will emit warnings.
  # Enable PMX_STRICT_ROUTING_CONFIG=1 to fail fast when these are present.
  enable_samossa: true
  enable_sarimax: true
  enable_garch: true
  enable_mssa_rl: true

  # Time Series signal generation thresholds (production defaults)
  time_series:
    confidence_threshold: 0.55  # Production threshold; requires meaningful model conviction
    min_expected_return: 0.0030 # 30 bps = 2x roundtrip cost buffer (actual ~15 bps roundtrip)
    max_risk_score: 0.70        # Conservative production risk ceiling
    use_volatility_filter: true # Filter signals based on volatility
    # Cost/uncertainty gating for Time Series signals.
    # - default_roundtrip_cost_bps is a *prior* used only when bid/ask or synthetic microstructure
    #   columns (TxnCostBps/ImpactBps) are unavailable. Values are basis points of notional.
    # - min_signal_to_noise: CI-based SNR gate. Requires expected_return > threshold * CI_half_width.
    #   When GARCH does not converge, CI widens -> SNR drops -> signal blocked automatically.
    cost_model:
      # Phase 7.9: Updated to realistic 2026 execution costs (per risk_mode.yml)
      default_roundtrip_cost_bps:
        US_EQUITY: 1.5   # Updated from 3.2 - modern smart order routing
        INTL_EQUITY: 3.0 # Updated from 6.0 - improved int'l execution
        FX: 1.0          # Updated from 2.0 - tight spreads on majors
        CRYPTO: 8.0      # Updated from 12.0 - CEX improvements
        INDEX: 1.5       # Updated from 2.0 - ETF liquidity
        UNKNOWN: 3.0     # Updated from 5.0 - less conservative default
      min_signal_to_noise: 1.5  # Require E[return] > 1.5x CI half-width; 0 disables
    # Production per-ticker overrides.
    per_ticker:
      "CL=F":
        confidence_threshold: 0.55
        min_expected_return: 0.005
      "MSFT":
        confidence_threshold: 0.55
        min_expected_return: 0.0020  # 20 bps floor; 5 bps was below roundtrip cost
        max_risk_score: 0.70
      "MTN":
        confidence_threshold: 0.55
        min_expected_return: 0.0020
      "AAPL":
        confidence_threshold: 0.55  # Reset to global default; 0.60 override was uncalibrated ("temporary" label, no evidence)
        min_expected_return: 0.0030  # Reset to global default; 80 bps was 2.7× global floor with no evidence basis
      "GS":
        confidence_threshold: 0.55  # Reset to global default; 0.65 override was uncalibrated ("temporary" label, no evidence)
        min_expected_return: 0.0030  # Reset to global default; 120 bps had no evidence basis

    # Research/diagnostic overrides live in automation scripts via DIAGNOSTIC_MODE/TS_DIAGNOSTIC_MODE=1
    # so production thresholds stay conservative while evidence is gathered.

  # Fallback triggers
  ts_fallback_triggers:
    - forecast_unavailable
    - confidence_below_threshold
    - model_failure
    - insufficient_data

  execution:
    # No-trade windows by asset class (24h clock, inclusive of start, exclusive of end)
    no_trade_windows:
      US_EQUITY: []  # e.g., ["09:30-09:45", "15:50-16:00"]
      FX: []         # e.g., ["16:00-18:00"]
    enhanced_logging:
      include_mid_price: true
      include_route_type: true
      include_fill_timestamp: true

  # Deprecated routing mode hint (currently no-op; runtime derives mode from active flags).
  routing_mode: "TIME_SERIES_PRIMARY_LLM_FALLBACK"  # Options:
  # - TIME_SERIES_PRIMARY_LLM_FALLBACK (default)
  # - TIME_SERIES_PRIMARY_LLM_REDUNDANCY
  # - TIME_SERIES_ONLY
  # - LLM_ONLY (legacy mode)

  directional_classifier:
    enabled: false                    # Phase 9: inactive until training set >= 60 labeled examples
    p_up_threshold_buy: 0.55          # P(price_up) must exceed this for BUY to pass gate
    p_up_threshold_sell: 0.55         # P(price_down) = 1 - p_up must exceed this for SELL
