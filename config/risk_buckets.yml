risk_buckets:
  enabled: false  # feature flag; when false, allocator is a pass-through
  base_nav_frac:
    safe: 0.80
    ts_core: 0.12
    speculative: 0.05
    ml_secondary: 0.02
    llm_fallback: 0.01
  min_nav_frac:
    safe: 0.65
    ts_core: 0.08
    speculative: 0.02
    ml_secondary: 0.0
    llm_fallback: 0.0
  max_nav_frac:
    safe: 0.95
    ts_core: 0.20
    speculative: 0.10
    ml_secondary: 0.05
    llm_fallback: 0.02
  notes: >
    Opposite-risk sleeve template: large safe reserve, small speculative sleeve,
    and modest TS/ML overlays. Allocator stays disabled until wired and tested in
    the live production path.
