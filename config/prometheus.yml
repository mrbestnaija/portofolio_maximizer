global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - prometheus_alert_rules.yml

scrape_configs:
  - job_name: pmx_observability
    static_configs:
      - targets:
          - 127.0.0.1:9108
